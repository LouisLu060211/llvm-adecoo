四种全过,包括 uint8。所以最小复现失败了,单纯的 fp32 store 到 uint8 是支持的。

这反而是有价值的信息。报错里是 `uint32_t_to_uint8_t`,源头不是 fp32,是 uint32。说明触发条件在算子链上游某个地方产生了 uint32 中间值,然后才截断到 uint8。我的脚本没有那一步,所以走的是另一条 lowering 路径。

要缩小范围,得找出链上哪一步引入了 uint32。**在 A5 上:**

```bash
sed -n '1,84p' /home/yaolu/CP-0086.py
```

看 IS_UINT 在上游怎么影响类型,尤其是矩阵乘法、条件选择、GELU 那几步里有没有显式的 cast 或者 int 运算。

另一个更直接的办法:你手上有现成的 ttadapter MLIR,直接在里面找那个 vcast:

```bash
ls /home/yaolu/save_path/save_path/ | head
grep -rn "uint32\|i32" /home/yaolu/save_path/save_path/*/  2>/dev/null | grep -i vcast | head
```

或者从失败用例的 dump 目录里拿:

```bash
grep -l "vcast" /root/.triton/dump/*/kernel.ttadapter.mlir 2>/dev/null | head -3
```

拿到那个 .mlir 之后单独喂给 bishengir-compile,就是一个不需要跑 Python 的最小复现,对修的人最有用。

不过话说回来,工单填报所需的信息你已经齐了:253 个用例、23 个失败全是 uint8、唯一报错、版本号、以及 11 天后仍存在。最小复现是加分项。如果时间紧就先把工单提了,把"最小复现尝试过但单条 store 不触发,需要上游 uint32 中间值"这一点写进定位信息,这本身也是有用的线索。
