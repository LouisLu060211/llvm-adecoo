bash
cd ~/work/ascend/AscendNPU-IR
git fetch origin refs/merge-requests/3160/head:mr-3160-v2 -f
git log --oneline -8 mr-3160-v2
git diff mr-3160 mr-3160-v2 --stat

If the new head has the option, switch to it and drop your constexpr stub, since it hardcodes to true exactly what the pass is meant to control.

Meanwhile your greps returned 0 because the log has no multibuffer marks. Check what's actually in it:

bash
grep -c "PassID" ~/mr3160_dump.log
grep -in "multi_buffer\|multibuffer" ~/mr3160_dump.log | head
grep -in "tightly_coupled\|TightlyCoupled" ~/mr3160_dump.log | head
grep -in "preload" ~/mr3160_dump.log | head
