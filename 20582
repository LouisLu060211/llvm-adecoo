1. 拿 VecAdd 的 IR 喂给 bishengir-opt，验证工具通不通：

bash
./build/bin/bishengir-opt bishengir/test/Integration/HIVM/VecAdd/add.mlir -o /tmp/out.mlir && echo "OK" && head -20 /tmp/out.mlir

打印 OK 并出来一段 IR，就说明编译器工具本身是好的。

2. 顺便看一眼这个例子的 IR 长什么样：

bash
cat bishengir/test/Integration/HIVM/VecAdd/add.mlir

3. 确认 A3、A5 到底是啥（上一条你没跑，这次单独跑）：

bash
file A3 A5

4. 试着让官方测试框架自己跑（如果 lit 装好了，它会自动跳过需要硬件的用例，只跑能跑的）：

bash
./build/bin/llvm-lit -a bishengir/test/Integration/HIVM/VecAdd/ 2>&1 | head -30
