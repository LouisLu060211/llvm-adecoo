Verify, then copy. From PowerShell:

```powershell
ssh a5 "hostname; df -h /"
```

If that prints `tbe` and disk stats, you're through. Then:

```powershell
scp -3 -r fremont:Q3TritonKernel a5:/home/yaolu/
```

Then get on the A5 and run the test:

```powershell
ssh a5
```

```bash
source /home/yaolu/env.sh
cd /home/yaolu/Q3TritonKernel
ls
pytest -s tests/fla_perf/test_nsa_compression_kernels_perf.py::test_nsa_compression_fwd_kernel_perf
```

Paste the test output. Watch the disk number in that first command too.
