两个问题。

**一是 RUN 行没有 `-verify-diagnostics`**，所以 `expected-error` 不起作用。

**二是刚才那次 heredoc 又被吃了字符**,输出里 `head -3` 那行和测试内容混在一起了，说明写进去的内容是坏的。

先撤掉：

```bash
git checkout mlir/test/Conversion/XeGPUToXeVM/loadstore_matrix.mlir
```

## 换个做法

`-verify-diagnostics` 加到现有 RUN 行会影响整个文件，风险大。单独建一个测试文件更干净：

```bash
python3 - <<'PYEOF'
content = '''// RUN: mlir-opt -split-input-file -convert-xegpu-to-xevm -verify-diagnostics %s

// A result with more than one non-unit dimension cannot be flattened into the
// single contiguous access this lowering emits. Report it instead of tripping
// an assertion.
// See https://github.com/llvm/llvm-project/issues/208902.

gpu.module @test_kernel [#xevm.target<chip = "pvc">] {
  gpu.func @load_matrix_2d_result(%arg0: memref<4x8xf32>) kernel {
    %alloca = memref.alloca() : memref<4x8xf32, 3>
    %mdesc = xegpu.create_mem_desc %alloca : memref<4x8xf32, 3> -> !xegpu.mem_desc<4x8xf32>
    // expected-error@+1 {{failed to legalize operation 'xegpu.load_matrix'}}
    %res = xegpu.load_matrix %mdesc[0, 0] : !xegpu.mem_desc<4x8xf32> -> vector<4x8xf32>
    %c0 = arith.constant 0 : index
    vector.store %res, %arg0[%c0, %c0] : memref<4x8xf32>, vector<4x8xf32>
    gpu.return
  }
}
'''
open('mlir/test/Conversion/XeGPUToXeVM/loadstore_matrix_invalid.mlir', 'w').write(content)
print("written")
PYEOF
cat mlir/test/Conversion/XeGPUToXeVM/loadstore_matrix_invalid.mlir
```

先看 `cat` 出来的内容完整不完整，特别是带 `!` 的那两行。完整了再跑：

```bash
build/bin/llvm-lit -v mlir/test/Conversion/XeGPUToXeVM/loadstore_matrix_invalid.mlir
```

输出发我。
