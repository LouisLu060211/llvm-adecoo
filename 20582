Then re-run only those two cases in isolation:

bash
cd /home/yaolu/Q3TritonKernel
pytest -s "tests/fla_perf/test_nsa_compression_kernels_perf.py::test_nsa_compression_fwd_kernel_perf[B4-T1024-H4-HQ64-K32-V32-BS64-scale0.1-bfloat16]"

If the bimodality vanishes when the box is quiet, your baseline for those two cases was measurement noise and the real target is B2-T8192 alone.

And the static analysis you skipped. On the A5:

bash
ls /root/.triton/dump/ | head
du -sh /root/.triton/dump/
