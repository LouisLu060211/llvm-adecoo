bash
uptime
which bishengir-compile
bishengir-compile --version

The version string should say 6e21948ae932. If it says something else, you measured the wrong binary.

Then re-run with the daily package on the same machine at the same time, which is the only clean control:

bash
cd /home/yaolu/Q3TritonKernel
env PATH="/data/ci_env/daily/pkg/compiler/tools/bishengir/bin:/data/ci_env/daily/pkg/ccec/bin:$PATH" \
    BISHENG_INSTALL_PATH=/data/ci_env/daily/pkg/ccec/bin \
  pytest -s "tests/fla_perf/test_nsa_compression_kernels_perf.py::test_nsa_compression_fwd_kernel_perf[B4-T1024-H4-HQ64-K32-V32-BS64-scale0.1-bfloat16]" 2>&1 | tail -5

Then point detail2.py at that new timestamp. If the daily build gives ~827us right now and yours gives 645us, the improvement is real. If the daily build also gives 645us today, the difference is the machine, not the patch.

Your 827.6 baseline came from September 4 at load 48, so a same-session control matters.

Also grab the dump hash for this run so you can do the IR comparison:

bash
ls -lt /root/.triton/dump/ | head -5
