Missing the `.py` extension. Also worth confirming the file actually arrived:

```bash
ls -la /home/yaolu/CP-0086.py
ls /home/yaolu/save_path
```

Then:

```bash
cd /home/yaolu
python CP-0086.py 2>&1 | tee ~/CP-0086-repro.log
tail -40 ~/CP-0086-repro.log
```

One thing worth flagging about the version. The daily package now reports `0fbc62995d13` built 2026-09-11, today. The ticket was filed against an 8.31 build, eleven days older. If it doesn't reproduce, that's ambiguous: could be fixed, could be masked by other changes. Note the version you tested either way, since the ticket asks for 版本信息.
