Finish the comparison:

bash
grep -oE "memref\.alloc\([^)]*\) *(\{[^}]*\})? *: memref<[^>]*\?[^>]*>" ~/base_dump.log | sort | uniq -c | sort -rn
grep -cE 'hivm\.multi_buffer = 2|annotation\.mark.*multi_buffer' ~/base_dump.log
grep -cE 'arith\.select.*memref<.*#hivm\.address_space<ub>' ~/base_dump.log

Then restore the patch and note the result:

bash
exit
cd ~/work/ascend/AscendNPU-IR
git stash pop
git diff --stat
