Same output pasted again. What I need is one line:

bash
ls -la build-ubuntu/bin/bishengir-compile

That timestamp decides everything. Fresh means mr-3236 built and only hivmc-a5 failed, so you carry on. Old means you're blocked and need:

bash
ninja -C build-ubuntu bishengir-compile

which skips hivmc-a5 entirely.

Also worth confirming, since it's the reason you switched branches:

bash
grep -rn "preserveTightlyCoupledLayout" bishengir/include/bishengir/Dialect/HIVM/Transforms/Passes.td

Paste those two.
