Next, redo both on B2-T8192. On the A5:

bash
cd /home/yaolu/Q3TritonKernel
pytest -s "tests/fla_perf/test_nsa_compression_kernels_perf.py::test_nsa_compression_fwd_kernel_perf[B2-T8192-H4-HQ64-K64-V64-BS32-scale0.1-float16]"

Note the winning config it prints and the dump hash. Then check whether it's still ~430000us on a quiet machine:

bash
cd /home/yaolu/Q3TritonKernel/result_dir
ls -dt *B2-T8192* | head -1

Point detail2.py at that new directory by editing the timestamp in the glob.

If it drops to something proportional, roughly 6600us given 2x the tokens of B4-T2048's 1666 with half the batch, then the whole thing was contention and there's no bug. If it stays at 430000, that's your real target and static analysis on its dump is the next step.

Before you run it, check the box is actually quiet:

bash
uptime

Yesterday's numbers are unusable because you didn't have that. Record it alongside every measurement from now on.
