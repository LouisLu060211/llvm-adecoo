LGTM 加一个小要求，照做就行。

## 回复

```
Done, thanks!
```

## 先把注释改掉

我写的那段五行注释确实啰嗦。改成一行：

```bash
python3 - <<'PYEOF'
SRC = "mlir/lib/Conversion/XeGPUToXeVM/XeGPUToXeVM.cpp"
s = open(SRC).read()
OLD = """        // This pattern emits a single contiguous access, so the result has to
        // be flattenable to a 1D vector. A result with more than one non-unit
        // dimension is valid IR but is not something this lowering can
        // express, so bail out instead of asserting.
        if (llvm::count_if("""
NEW = """        // Only a result flattenable to 1D can be a single contiguous access.
        if (llvm::count_if("""
assert s.count(OLD) == 1, s.count(OLD)
open(SRC, "w").write(s.replace(OLD, NEW))
print("done")
PYEOF
```

如果他的意思是注释全删，那就把 `NEW` 里那行注释也去掉。一行的版本我觉得该留,不然读代码的人不知道为什么有这个限制。他说的是 "verbose"，不是 "remove all comments"。

## 然后

```bash
ninja -C build mlir-opt
git add -A
git clang-format
git add -A
git commit --amend --no-edit
git push --force-with-lease
```

`--amend` 把改动并进原 commit。也可以新开一个 commit，合并时会 squash，两种都行。

推完再回 `Done, thanks!` 并 Resolve conversation。
