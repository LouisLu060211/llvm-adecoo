## 确认死代码没了

```bash
build/bin/mlir-opt /tmp/input.mlir '--pass-pipeline=builtin.module(spirv-attach-target{ver=v1.3 caps=Shader exts=SPV_KHR_storage_buffer_storage_class},test-convert-to-spirv{convert-gpu-modules=true nest-in-gpu-module=true},gpu.module(spirv.module(spirv-lower-abi-attrs,spirv-update-vce)))'
```

数一下 `spirv.AccessChain` 的个数,应该是 8 个（load 4 个 + store 4 个），不再有多余的。

输出发我。

## 然后更新测试

`mlir/test/Conversion/VectorToSPIRV/vector-to-spirv.mlir` 里 `vector_load` 和 `vector_store` 相关的用例，CHECK 还写着 `spirv.Bitcast`，要改成新的输出。

先看有几个受影响：

```bash
build/bin/llvm-lit mlir/test/Conversion/VectorToSPIRV/ 2>&1 | grep -c "error: no match"
```

两条都跑，输出发我。
