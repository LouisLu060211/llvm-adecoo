复现完成,253 个用例 230 过 23 失败。现在把失败的 23 个筛出来,看规律。

**在 A5 上:**

```bash
grep -n "\[ERROR\]" ~/CP-0086-run2.log | head -30
grep -oE "dtype=[a-z0-9]+" ~/CP-0086-run2.log | sort | uniq -c
```

第二条能看出总体分布,但更关键的是只看失败的:

```bash
grep "\[ERROR\]" ~/CP-0086-run2.log | grep -oE "dtype=[a-z0-9]+, ndim=[0-9]+, shape=\([^)]*\)" | sort
```

以及报错类型有几种:

```bash
grep -oE "don't support cast [a-z0-9_]+" ~/CP-0086-run2.log | sort | uniq -c
grep -c "ConvertLinalgRToBinary" ~/CP-0086-run2.log
```

从你之前看到的那一条,错误是 `uint32_t_to_uint8_t_truncmode` 不支持,失败例子是 `dtype=uint8`。如果 23 个全是 uint8,那工单标题写的"部分 shape"其实不准确,真正的规律是 dtype 而不是 shape,这点值得在工单里更正。

另外日志里有个东西值得注意:`SSBUFFER return code=2, will fallback to enable_dynamic_cv_pipeline=False`。每次编译都出现,连成功的用例也有。这和你前几天在 3236 上看的 SSBuffer 与 preload 那条线是同一块东西,不一定和这个工单相关,但如果后面要问 renxingdong,可以顺带提。

把上面四条的输出贴上来,我帮你整理成工单要填的 现象描述 和 初步结论。
