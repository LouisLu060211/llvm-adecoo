git log -S came back empty, so no commit in your fetched history ever introduces preserveTightlyCoupledLayout. The patch is incomplete, not misapplied.

The context also tells you what shape the missing piece has. matchAndRewrite is a const member of ConstantizeAllocLikeOp, and that struct inherits its constructor with using OpRewritePattern<AllocLikeOp>::OpRewritePattern; and declares no members. So preserveTightlyCoupledLayout would have to be either a new member on that pattern or a pass option threaded through. Neither exists.

Check the remaining pieces. On fremont:

bash
cd ~/work/ascend/AscendNPU-IR
grep -rn "getAnnotatedBufferSizeInBytes" bishengir/ | head
grep -rn "ConstantizeBufferSize" bishengir/include/bishengir/Dialect/HIVM/Transforms/Passes.td

The first matters because your earlier grep was truncated by head, so I can't tell whether that helper exists either. The second shows whether the pass declares any options in tablegen, which is where a preserve-tightly-coupled-layout option would live.

Also useful:

bash
sed -n '/void ConstantizeBufferPass::runOnOperation/,/^}/p' bishengir/lib/Dialect/HIVM/Transforms/ConstantizeBufferSize.cpp
