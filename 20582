Check whether it exists. On fremont:

bash
cd ~/work/ascend/AscendNPU-IR
grep -rn "isManualVFScope" bishengir/ | head

If you find a definition in a .cpp, check that file is in its CMakeLists, same as before.

Notice the rpath in that link line though: -Wl,-rpath-link,/home/yaolu/work/ascend/AscendNPU-IR/build_hivmc/./lib. That points at build_hivmc, not build-ubuntu. Some cached cmake state is referring to a directory you don't use. Check:

bash
ls -d ~/work/ascend/AscendNPU-IR/build*

If build_hivmc exists and is stale, or doesn't exist at all, your build directory is carrying configuration from a different layout. That would explain symbols resolving inconsistently.

Given three missing symbols in a row, the cleanest move is a clean build directory:
